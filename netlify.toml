[build]
  command = "next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Scheduled function - runs daily at 9am UTC
# Triggers auto follow-up sequences via /api/sequences
